# 🚀 Docker Quick Start

## Подготовка за 3 шага

```bash
# 1. Создать конфиг
make setup

# 2. Отредактировать .env.prod
# Заполни BOT_TOKEN, OPENAI_API_KEY, POSTGRES_PASSWORD и остальное

# 3. Запустить
make up
```

## Основные команды

```bash
make up          # Запустить все
make down        # Остановить все
make logs        # Посмотреть логи
make restart     # Перезапустить все
make ps          # Статус сервисов
make health      # Проверить здоровье
```

## Логи отдельных сервисов

```bash
make logs-bot    # Логи бота
make logs-api    # Логи API
make logs-db     # Логи PostgreSQL
```

## Работа с кодом

```bash
# После изменения кода:
make rebuild

# Только бот:
docker-compose restart bot

# Только API:
docker-compose restart api
```

## Работа с БД

```bash
make backup      # Создать бэкап
make restore     # Восстановить последний бэкап
make shell-db    # Подключиться к psql
```

## Отладка

```bash
make shell-bot   # Зайти в контейнер бота
make shell-api   # Зайти в контейнер API
make stats       # Использование ресурсов
```

## Разные окружения

```bash
ENV=prod make up    # Production
ENV=test make up    # Test
ENV=dev make up     # Development
```

(Создай соответствующие .env.prod, .env.test, .env.dev)

## Первый запуск

```bash
# 1. Setup
make setup

# 2. Отредактируй .env.prod
nano .env.prod

# 3. Запусти
make up

# 4. Проверь логи
make logs

# 5. Проверь здоровье
make health
```

## Полная очистка (УДАЛИТ БД!)

```bash
make clean-all
```

---

**Вот и всё. Docker magic! ✨**

