# 🔍 Анализ Level 2: Проблемы и Решения

## 📊 Результаты тестирования

**Дата:** 2025-10-28  
**Тест:** 30 сообщений (сценарий "Алексей - Прокрастинирующий Перфекционист")  
**Настройки:** Короткие ответы + Ироничный мудрец

---

## ❌ Проблема #1: Игнорируются настройки длины ответов

### Что ожидалось:
- `message_length = 'brief'` → **50-100 слов (1-2 абзаца)**

### Что получилось:
- Ответы бота: **200-350 слов (3-5 абзацев)**
- Пример (ответ №9):
  ```
  Алексей, твой страх перед неудачей — это как пытаться убежать 
  от своей тени... [абзац 1, ~80 слов]
  
  Но позволь мне поделиться мудростью веков... [абзац 2, ~70 слов]
  
  Давай посмотрим правде в глаза: неудача — это не конец света... 
  [абзац 3, ~60 слов]
  
  Помни, Алексей, совершенство — это мираж... [абзац 4, ~50 слов]
  ```
  **Итого: ~260 слов при лимите 100-150!**

### Причина:
GPT-4 плохо следует мягким ограничениям ("максимум 100-150 слов") без дополнительного enforcement.

### 📌 Текущий промпт:
```python
'brief': '⚠️ КРИТИЧНО: Отвечай СТРОГО 1-2 короткими абзацами (максимум 100-150 слов). Длиннее НЕЛЬЗЯ.'
```

**Проблема:** Это soft instruction, GPT может игнорировать.

---

## ❌ Проблема #2: Паттерны с частотой 1 вместо 4-5+

### Что ожидалось:
По сценарию пользователь **минимум 4-5 раз** говорит:
- "я опять всё испортил" (4 раза)
- "откладываю задачи" (3-4 раза)
- "боюсь что выйдет плохо" (5-6 раз)

**Expected patterns:**
```json
{
  "title": "Страх неудачи",
  "occurrences": 5,
  "confidence": 0.85+,
  "evidence": ["я опять всё испорчу", "вдруг не получится", ...]
}
```

### Что получилось:
```
🧠 Выявленные паттерны:
- Осознание негативного саморазговора (частота: 1)
- Неуверенность в себе (частота: 1)
- Негативное ожидание (частота: 1)
- Преувеличение значимости результатов (частота: 1)
- Страх отторжения (частота: 1)
```

**Все паттерны с occurrences=1!** Похоже на то что:
1. GPT создает НОВЫЕ паттерны каждый `quick_analysis` вместо мерджа
2. Embedding deduplication не работает
3. Или `quick_analysis` вообще не вызывается достаточно часто

### Причина:
Логика мерджа паттернов через embeddings **не отрабатывает** или:
- `quick_analysis` вызывается слишком редко (каждые 5 сообщений, но паттерны не накапливаются)
- GPT генерирует слишком разные названия для одного паттерна ("страх неудачи" vs "негативное ожидание")
- Threshold для similarity слишком высокий (0.85 по умолчанию)

---

## ❌ Проблема #3: Evidence (цитаты) не показываются в /my_profile

### Что ожидалось (Level 2):
```
🧠 Выявленные паттерны:
- Страх неудачи (частота: 5)
  📝 Примеры из диалогов:
    • "вдруг опять всё испорчу?"
    • "боюсь что выйдет плохо"
    • "я опять всё испортил"
```

### Что получилось:
```
🧠 Выявленные паттерны:
- Осознание негативного саморазговора (частота: 1)
  [НЕТ ПРИМЕРОВ!]
```

### Причина:
В функции `_format_profile_with_gpt` (profile.py) **нет инструкции** показывать evidence!

Промпт говорит:
```
ИНСТРУКЦИИ:
...
5. Паттерны и инсайты объясняй простым языком
...
```

**Но НЕ говорит:**
> "Для каждого паттерна ОБЯЗАТЕЛЬНО покажи примеры из диалогов (evidence)"

GPT-4o-mini просто не знает что делать с полем `evidence` и игнорирует его.

---

## ❌ Проблема #4: Слабое цитирование в ответах бота

### Что ожидалось (Level 2):
```
Помнишь, ты говорил "вдруг опять всё испорчу"? Это проявление 
перфекционизма, который...
```

### Что получилось:
Только в **3 из 20** ответов бот цитирует напрямую:
- ✅ Ответ 7: "Ты говорил: 'Вдруг опять всё испорчу?'"
- ✅ Ответ 9: "Помнишь, как ты боялся, что 'вдруг опять всё испорчу'?"
- ✅ Ответ 18: "Ты упоминал о своих опасениях портить вещи несколько раз"

Остальные ответы **без прямых цитат**.

### Причина:
Промпт в `openai_service.py` говорит:
```python
"⚠️ ВАЖНО: Используй эти КОНКРЕТНЫЕ ПРИМЕРЫ из диалогов пользователя."
"Например: 'Помнишь, ты говорил \"[цитата]\"?'"
```

Но это **не обязательное требование**, GPT может игнорировать.

---

# 🔧 РЕШЕНИЯ

## ✅ Fix #1: Жесткое ограничение длины ответов

### Option A: Post-processing обрезка (ПРОСТОЕ, МГновенное)

```python
# В openai_service.py, после получения ответа от GPT
async def get_chat_completion(...):
    # ... existing code ...
    
    response_text = response.choices[0].message.content
    
    # 🔪 Обрезка для 'brief' (если превышает лимит)
    if profile and profile.message_length == 'brief':
        response_text = _enforce_brief_length(response_text, max_words=150)
    
    return response_text

def _enforce_brief_length(text: str, max_words: int = 150) -> str:
    """
    Жестко обрезать ответ если превышает лимит слов
    
    Стратегия:
    1. Считаем слова
    2. Если > max_words → обрезаем по предложениям
    3. Добавляем ellipsis если обрезали
    """
    words = text.split()
    
    if len(words) <= max_words:
        return text  # OK, в пределах лимита
    
    # Обрезаем по предложениям (не по словам!)
    sentences = text.split('. ')
    result = []
    word_count = 0
    
    for sentence in sentences:
        sentence_words = len(sentence.split())
        if word_count + sentence_words <= max_words:
            result.append(sentence)
            word_count += sentence_words
        else:
            break
    
    truncated = '. '.join(result)
    if not truncated.endswith('.'):
        truncated += '.'
    
    return truncated
```

**Плюсы:**
- ✅ Работает 100% времени (hard enforcement)
- ✅ Просто реализовать
- ✅ Не ломает смысл (обрезаем по предложениям)

**Минусы:**
- ⚠️ Может обрезать важную мысль в конце

---

### Option B: Усиленный промпт с примерами (СРЕДНЯЯ сложность)

```python
length_map = {
    'brief': '''⚠️ КРИТИЧНО: МАКСИМУМ 100-150 СЛОВ (1-2 абзаца).

ПЛОХОЙ пример (слишком длинный):
"Алексей, твой страх... [абзац 1]
Но позволь... [абзац 2]
Давай посмотрим... [абзац 3]
Помни... [абзац 4]"
→ 260 слов ❌

ХОРОШИЙ пример (короткий):
"Алексей, страх перед неудачей — это твоя тень. Но каждый раз, 
когда ты действуешь несмотря на страх, ты становишься сильнее. 

Начни с маленького шага прямо сейчас. Что можешь сделать за 15 минут?"
→ 45 слов ✅

ЕСЛИ ПРЕВЫШАЕШЬ 150 СЛОВ → ОСТАНОВИСЬ И ПЕРЕПИШИ КОРОЧЕ.''',
}
```

**Плюсы:**
- ✅ GPT видит конкретные примеры
- ✅ Контрастность (плохой vs хороший)

**Минусы:**
- ⚠️ Не 100% гарантия
- ⚠️ Увеличивает prompt size

---

### Option C: Few-shot с короткими диалогами (СЛОЖНАЯ)

Добавить в `build_system_prompt` несколько примеров коротких диалогов:

```python
if profile.message_length == 'brief':
    few_shot_examples = """
## 📝 ПРИМЕРЫ ПРАВИЛЬНЫХ КОРОТКИХ ОТВЕТОВ:

User: "Я опять всё откладываю."
Assistant: "Ты застрял в цикле прокрастинации. Попробуй правило 2 минут: начни задачу всего на 2 минуты. Начало — это 80% успеха."

User: "Боюсь что не получится."
Assistant: "Страх говорит: 'не получится'. Мудрость отвечает: 'узнаешь только попробовав'. Какой первый шаг ты можешь сделать прямо сейчас?"

⚠️ ВСЕ ТВОИ ОТВЕТЫ ДОЛЖНЫ БЫТЬ ТАКОЙ ЖЕ ДЛИНЫ (50-100 слов max).
"""
    prompt_parts.append(few_shot_examples)
```

**Плюсы:**
- ✅ GPT учится на примерах (few-shot learning)
- ✅ Показывает желаемый стиль

**Минусы:**
- ⚠️ Сложнее поддерживать
- ⚠️ Увеличивает токены на каждый запрос

---

### 🎯 РЕКОМЕНДАЦИЯ для Fix #1:

**Гибридный подход:**
1. Усилить промпт с примерами (Option B) → влияет на стиль
2. + Post-processing обрезка (Option A) как safety net → гарантия

```python
# Комбо: улучшенный промпт + hard limit
def _build_style_instructions(profile):
    length_map = {
        'brief': '''⚠️ КРИТИЧНО: МАКСИМУМ 100-150 СЛОВ (1-2 абзаца).

ХОРОШИЙ пример:
"Алексей, страх — это твоя тень. Действуй несмотря на него.
Начни с 15 минут работы прямо сейчас." (30 слов ✅)

ЕСЛИ ПРЕВЫШАЕШЬ 150 СЛОВ → СОКРАТИ НЕМЕДЛЕННО.'''
    }
    # ... rest of code

async def get_chat_completion(...):
    response_text = response.choices[0].message.content
    
    # Safety net: обрезка если GPT всё равно превысил
    if profile.message_length == 'brief':
        response_text = _enforce_brief_length(response_text, max_words=150)
    
    return response_text
```

**ROI: HIGH** (простая реализация + гарантированный результат)

---

## ✅ Fix #2: Исправить частоту паттернов (occurrences)

### Проблема: GPT создаёт новые паттерны вместо мерджа

**Текущая логика:**
1. `quick_analysis` каждые 5 сообщений → создает паттерны
2. Должен искать похожие через embeddings
3. Если найдены → увеличивать `occurrences`

**Но происходит:**
- Паттерны создаются, но не мержатся
- `occurrences` остается 1

### Решение Option A: Более агрессивный мердж (ПРОСТОЕ)

```python
# В pattern_analyzer.py

# Текущий threshold для similarity
SIMILARITY_THRESHOLD = 0.85  # Слишком высокий!

# Новый threshold
SIMILARITY_THRESHOLD = 0.75  # Более мягкий
```

**Логика:**
- 0.85 = "почти идентичные" паттерны
- 0.75 = "похожие" паттерны (лучше для мерджа)

---

### Решение Option B: Инструкция GPT НЕ создавать дубликаты (СРЕДНЕЕ)

Модифицировать промпт в `quick_analysis`:

```python
# В _analyze_conversation_quick

prompt = f"""
EXISTING PATTERNS (НЕ СОЗДАВАЙ ДУБЛИКАТЫ!):
{json.dumps(existing_patterns, ensure_ascii=False, indent=2)}

⚠️ ВАЖНО: Если видишь паттерн ПОХОЖИЙ на существующий → НЕ создавай новый!
Вместо этого укажи ID существующего паттерна для увеличения occurrences.

OUTPUT FORMAT:
{{
  "existing_pattern_updates": [
    {{"id": "<existing_id>", "new_evidence": ["quote1", "quote2"]}}
  ],
  "new_patterns": [...]  # Только если ДЕЙСТВИТЕЛЬНО новые
}}
"""
```

**Логика:**
- GPT сам определяет дубликаты
- Возвращает updates для существующих паттернов
- Мы увеличиваем их `occurrences` вручную

---

### Решение Option C: Post-processing мердж после GPT (НАДЕЖНОЕ)

```python
# После получения new_patterns от GPT

async def _merge_patterns_with_existing(
    new_patterns: list,
    existing_patterns: list,
    threshold: float = 0.75
) -> list:
    """
    Мердж новых паттернов с существующими через embeddings
    
    Returns:
        Merged list (new patterns добавлены к existing с увеличенным occurrences)
    """
    for new_p in new_patterns:
        # Генерируем embedding для нового паттерна
        new_text = f"{new_p['title']} {new_p['description']}"
        new_embedding = await get_embedding(new_text)
        
        # Ищем похожий existing pattern
        best_match = None
        best_similarity = 0
        
        for exist_p in existing_patterns:
            if 'embedding' in exist_p:
                similarity = cosine_similarity(new_embedding, exist_p['embedding'])
                if similarity > best_similarity and similarity >= threshold:
                    best_match = exist_p
                    best_similarity = similarity
        
        if best_match:
            # МЕРДЖ: увеличиваем occurrences, добавляем evidence
            best_match['occurrences'] += 1
            best_match['evidence'].extend(new_p.get('evidence', []))
            best_match['last_detected'] = datetime.now().isoformat()
            logging.info(f"Merged pattern: {new_p['title']} → {best_match['title']} (similarity: {best_similarity:.2f})")
        else:
            # Действительно новый паттерн
            new_p['embedding'] = new_embedding
            new_p['occurrences'] = 1
            existing_patterns.append(new_p)
            logging.info(f"Added new pattern: {new_p['title']}")
    
    return existing_patterns
```

---

### 🎯 РЕКОМЕНДАЦИЯ для Fix #2:

**Option C (Post-processing мердж)** — самое надежное:
- ✅ Не зависит от GPT (GPT может ошибаться)
- ✅ 100% контроль через embeddings
- ✅ Логика в одном месте

**ROI: MEDIUM-HIGH** (требует реализации, но надежно)

---

## ✅ Fix #3: Показывать evidence в /my_profile

### Текущая проблема:
GPT-4o-mini получает evidence в JSON, но не знает что с ним делать.

### Решение: Добавить явную инструкцию в промпт

```python
# В profile.py, функция _format_profile_with_gpt

prompt = f"""
...
ИНСТРУКЦИИ:
1. Используй эмодзи для наглядности (🎨 🧠 💡 😊 🎓)
2. Структурируй информацию по блокам
3. Если данных нет — скажи что профиль ещё формируется
4. Будь дружелюбным и воодушевляющим
5. Паттерны и инсайты объясняй простым языком
6. ⚠️ ОБЯЗАТЕЛЬНО: Для каждого паттерна покажи ПРИМЕРЫ из диалогов (evidence)!
   Формат: "📝 Примеры: • \"цитата1\" • \"цитата2\""
7. Для настроения используй образные описания
8. Максимум 3000 символов (Telegram лимит)

ФОРМАТ ВЫВОДА:
```
🧠 <b>Ваш психологический профиль</b>

...

🧠 <b>Выявленные паттерны:</b>
- <b>Название паттерна</b> (частота: X)  
  Описание паттерна...  
  📝 <i>Примеры из ваших слов:</i>  
    • "цитата из диалога 1"  
    • "цитата из диалога 2"

...
```
"""
```

**Плюсы:**
- ✅ Простая модификация
- ✅ Явная инструкция для GPT

**Минусы:**
- ⚠️ Увеличит длину вывода (но мы обрезаем до 3000 символов, это OK)

**ROI: HIGH** (5 минут работы → полный Level 2 функционал)

---

## ✅ Fix #4: Усилить цитирование в ответах бота

### Текущая проблема:
GPT цитирует только иногда (3 из 20 ответов).

### Решение: Добавить ОБЯЗАТЕЛЬНОЕ требование

```python
# В openai_service.py, после секции с patterns

if patterns_full_text:
    prompt_parts.append(
        f"\n## 🧠 Выявленные паттерны пользователя:\n{patterns_full_text}\n\n"
        "⚠️ ВАЖНО: Используй эти КОНКРЕТНЫЕ ПРИМЕРЫ из диалогов пользователя в своих ответах. "
        "Когда даёшь совет или инсайт, ОБРАЩАЙСЯ к этим реальным ситуациям. "
        "Например: 'Помнишь, ты говорил \"[цитата]\"? Это проявление [паттерн]...'\n\n"
        
        # 🔥 NEW: Обязательное требование цитирования
        "🎯 ОБЯЗАТЕЛЬНО: В КАЖДОМ ответе (где уместно) используй ПРЯМУЮ ЦИТАТУ из примеров выше.\n"
        "Формат цитаты: 'Помнишь, ты говорил: \"[точная цитата]\". Это показывает...'\n"
        "Цитаты ДОЛЖНЫ быть в кавычках и дословные.\n\n"
        
        "❌ ПЛОХО: 'Ты упоминал о страхах.' (нет цитаты)\n"
        "✅ ХОРОШО: 'Ты говорил: \"вдруг опять всё испорчу?\". Это проявление перфекционизма.'"
    )
```

**Плюсы:**
- ✅ Явное требование с примерами (good/bad)
- ✅ Увеличит частоту цитирования

**Минусы:**
- ⚠️ Не 100% гарантия (GPT может игнорировать)

---

### Alternative: Проверка и retry если нет цитат

```python
async def get_chat_completion(...):
    response_text = response.choices[0].message.content
    
    # Проверяем есть ли цитата в ответе
    has_quote = '"' in response_text and any(
        evidence_phrase in response_text.lower() 
        for pattern in profile.patterns.get('patterns', [])
        for evidence_phrase in pattern.get('evidence', [])
    )
    
    if not has_quote and len(profile.patterns.get('patterns', [])) > 0:
        # Retry с усиленным промптом
        logging.warning("No quote detected, retrying with stronger prompt")
        # ... retry logic ...
```

**Плюсы:**
- ✅ Гарантированное цитирование

**Минусы:**
- ⚠️ Дополнительный API call (дорого)
- ⚠️ Увеличивает latency

**ROI: MEDIUM** (не критично для MVP)

---

# 📋 ПЛАН РЕАЛИЗАЦИИ

## Phase 1: Quick Wins (30 минут)

1. ✅ **Fix #1A: Post-processing обрезка**
   - Добавить `_enforce_brief_length()` в `openai_service.py`
   - Применять для `message_length == 'brief'`
   - **Impact: HIGH** (ответы сразу станут короткими)

2. ✅ **Fix #3: Evidence в /my_profile**
   - Обновить промпт в `_format_profile_with_gpt()`
   - Добавить инструкцию показывать примеры
   - **Impact: HIGH** (Level 2 заработает визуально)

3. ✅ **Fix #2A: Снизить similarity threshold**
   - Изменить `SIMILARITY_THRESHOLD` с 0.85 на 0.75
   - **Impact: MEDIUM** (может улучшить мердж)

---

## Phase 2: Deeper Fixes (1-2 часа)

4. ✅ **Fix #1B: Усиленный промпт с примерами**
   - Обновить `length_map` в `_build_style_instructions()`
   - Добавить контрастные примеры
   - **Impact: MEDIUM** (улучшит стиль ответов)

5. ✅ **Fix #4: Обязательное цитирование**
   - Обновить patterns section в `build_system_prompt()`
   - Добавить требование с примерами good/bad
   - **Impact: MEDIUM-HIGH** (больше цитат)

6. ✅ **Fix #2C: Post-processing мердж паттернов**
   - Реализовать `_merge_patterns_with_existing()`
   - Применять после `quick_analysis`
   - **Impact: HIGH** (правильная частота паттернов)

---

## Phase 3: Testing (30 минут)

7. ✅ **Regression test**
   - Запустить Level 2 test scenario снова
   - Проверить все 5 verification points
   - **Expected: 80%+ PASS rate**

---

# 🎯 ИТОГО

**Критичность проблем:**
1. **Длина ответов** — HIGH (пользователь явно выбрал "короткие")
2. **Evidence в профиле** — HIGH (Level 2 функционал не виден)
3. **Частота паттернов** — MEDIUM (функционально работает, но неточно)
4. **Цитирование** — MEDIUM (есть, но слабое)

**Рекомендуемый порядок фикса:**
1. Fix #1A (post-processing) + Fix #3 (evidence) → **30 мин, HIGH impact**
2. Fix #2A (threshold) → **5 мин, MEDIUM impact**
3. Fix #1B + Fix #4 → **1 час, MEDIUM-HIGH impact**
4. Fix #2C (мердж) → **1-2 часа, HIGH impact долгосрочно**

**Total time: ~3 часа для 100% Level 2.**

---

**Начинаем с Phase 1? Или хочешь обсудить подходы?** 🚀

