# 📋 Summary изменений — фикс переменных окружения

## Изменённые файлы

### ✏️ docker-compose.yml
**Проблема:** `environment: ${VAR}` брал переменные с хоста (где их нет), перезаписывая `.env.prod`

**Изменения:**
- ❌ Удалены все `${VAR}` интерполяции из секций `environment`
- ✅ Оставлены только Docker-специфичные override'ы:
  - `POSTGRES_HOST: postgres`
  - `POSTGRES_PORT: 5432`
- ✅ Все переменные теперь грузятся через `env_file: .env.prod`

**До:**
```yaml
environment:
  OPENAI_API_KEY: ${OPENAI_API_KEY}  # ← брало с хоста (пусто!)
  BOT_TOKEN: ${BOT_TOKEN}
  POSTGRES_HOST: postgres
```

**После:**
```yaml
env_file:
  - .env.prod
environment:
  POSTGRES_HOST: postgres  # Только Docker-специфичное
  POSTGRES_PORT: 5432
```

---

### ✏️ Makefile
**Проблема:** Дублирование `--env-file` в CLI и в docker-compose.yml

**Изменения:**
- ❌ Удалён флаг `--env-file .env.$(ENV)` из команд:
  - `build`
  - `up`
  - `up-fresh`
  - `rebuild`
  - `dev*`

**До:**
```makefile
rebuild:
	docker-compose --env-file .env.$(ENV) up -d --build
```

**После:**
```makefile
rebuild:
	docker-compose up -d --build
```

---

### ✏️ validate-env.sh
**Изменение:** Теперь всегда проверяет `.env.prod` (раз он hardcoded в docker-compose.yml)

**До:**
```bash
ENV=${ENV:-prod}
ENV_FILE=".env.${ENV}"
```

**После:**
```bash
ENV_FILE=".env.prod"
```

---

## Созданные файлы

### 📄 scripts/safe_redeploy.sh
**Назначение:** Безопасный редеплой БЕЗ потери данных БД

**Что делает:**
1. `git pull`
2. `docker-compose down` (останавливает контейнеры, но volumes остаются)
3. `docker rm -f ...` (удаляет старые метаданные контейнеров)
4. `docker-compose up -d --build` (пересобирает и запускает)
5. Показывает логи

**Использование:**
```bash
./scripts/safe_redeploy.sh
```

---

### 📄 DEPLOY_FIX.md
Детальное объяснение проблемы и решения

---

### 📄 ENV_VARS_FIX_SUMMARY.md
Техническая документация по исправлению

---

### 📄 SERVER_DEPLOY_INSTRUCTIONS.md
Полная инструкция по деплою на сервер (первый раз и последующие)

---

### 📄 DEPLOY_CHEATSHEET.md
Краткая шпаргалка — все команды на одной странице

---

### 📄 DOCKER_QUICKSTART.md (обновлён)
Добавлена секция "Деплой на сервере (БЕЗ потери БД)"

---

## Что изменилось в поведении

### До (плохо):
1. Docker Compose парсил `${OPENAI_API_KEY}` как переменную **окружения хоста**
2. На хосте этих переменных не было
3. Получались пустые строки → ошибки в боте
4. При изменении конфигурации — ошибка `KeyError: 'ContainerConfig'`

### После (хорошо):
1. Docker Compose загружает `.env.prod` через `env_file`
2. Все переменные попадают в контейнер **из файла**
3. Никаких warning'ов про missing variables
4. При редеплое БД не удаляется (если использовать `safe_redeploy.sh`)

---

## Единая точка правды

**Все переменные живут только здесь:**
```
/путь/к/проекту/.env.prod
```

**НЕ нужно:**
- ❌ Экспортировать переменные в shell хоста
- ❌ Копировать `.env.prod` в `soul_bot/`
- ❌ Создавать дополнительные `.env` файлы
- ❌ Передавать `--env-file` вручную

**Нужно:**
- ✅ Один файл `.env.prod` в корне
- ✅ Docker Compose сам его читает (через `env_file`)
- ✅ Использовать `./scripts/safe_redeploy.sh` для деплоя

---

## Миграция (что делать на сервере)

```bash
# 1. Сделать git pull (получить изменения)
git pull

# 2. Убедиться что .env.prod на месте
ls -la .env.prod

# 3. Заполнить placeholder'ы (если нужно)
nano .env.prod

# 4. Остановить старые контейнеры
docker-compose down

# 5. Удалить старые метаданные (фикс ContainerConfig)
docker rm -f soulnear_postgres soulnear_bot soulnear_api 2>/dev/null || true

# 6. Запустить с новой конфигурацией
make rebuild

# 7. Проверить логи
make logs-bot
```

---

## Проверка что всё работает

```bash
# Переменные НЕ должны быть пустыми
docker exec soulnear_bot env | grep -E "BOT_TOKEN|OPENAI_API_KEY"

# Логи БЕЗ ошибок
make logs-bot

# Статус: все сервисы UP
docker-compose ps
```

---

## Результат

- ✅ Нет warning'ов: `The ... variable is not set`
- ✅ Нет ошибки: `KeyError: 'ContainerConfig'`
- ✅ БД не удаляется при редеплое
- ✅ Все переменные грузятся из `.env.prod`
- ✅ Единая точка правды для конфигурации
- ✅ Скрипт `safe_redeploy.sh` для быстрого деплоя

---

**Красиво. Работает. Деплой за 5 секунд.** 🎉

