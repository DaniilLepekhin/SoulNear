# ✅ Исправление проблемы с переменными окружения

## 🔥 Проблема
При запуске `docker-compose up` на сервере появлялись ошибки:
```
WARNING: The POSTGRES_PASSWORD variable is not set. Defaulting to a blank string.
WARNING: The OPENAI_API_KEY variable is not set. Defaulting to a blank string.
ERROR: 'ContainerConfig'
```

## 🎯 Корень проблемы

Docker Compose обрабатывает переменные в **2 этапа**:

1. **При парсинге YAML** — интерполирует `${VAR}` из окружения **ХОСТА**
2. **При запуске контейнера** — загружает `env_file` **ВНУТРЬ** контейнера

**Конфликт:** Ты указал и `env_file: .env.prod`, И `environment: ${OPENAI_API_KEY}` → Docker пытался взять переменные с хоста (где их нет), перезаписывая значения из файла.

## ✅ Решение

### 1. docker-compose.yml — убраны дубли переменных

**Было (плохо):**
```yaml
bot:
  env_file:
    - .env.prod
  environment:
    OPENAI_API_KEY: ${OPENAI_API_KEY}  # ← перезаписывает значение из .env.prod пустотой!
    BOT_TOKEN: ${BOT_TOKEN}
    POSTGRES_HOST: postgres
```

**Стало (хорошо):**
```yaml
bot:
  env_file:
    - .env.prod  # ← все переменные оттуда
  environment:
    # Только Docker-специфичные override'ы
    POSTGRES_HOST: postgres
    POSTGRES_PORT: 5432
```

### 2. Makefile — убран дубль --env-file

**Было:**
```bash
docker-compose --env-file .env.prod up -d --build
```

**Стало:**
```bash
docker-compose up -d --build
```

Файл `.env.prod` уже указан в `env_file` внутри docker-compose.yml.

### 3. Создан скрипт safe_redeploy.sh

Безопасный редеплой БЕЗ потери данных БД:
```bash
#!/bin/bash
set -e

git pull
docker-compose down
docker rm -f soulnear_postgres soulnear_bot soulnear_api 2>/dev/null || true
docker-compose up -d --build
docker-compose logs -f --tail=50 bot
```

## 🚀 Как использовать

### На сервере (продакшн):

```bash
# Вариант 1 — автоматический скрипт
./scripts/safe_redeploy.sh

# Вариант 2 — вручную
docker-compose down && \
git pull && \
docker rm -f soulnear_postgres soulnear_bot soulnear_api 2>/dev/null || true && \
make rebuild && \
make logs-bot
```

### Локально (разработка):

```bash
make rebuild          # Пересобрать и запустить
make logs-bot         # Посмотреть логи бота
docker-compose ps     # Статус сервисов
```

## ⚠️ Важные заметки

### Где живут переменные?
**Только здесь:** `.env.prod` (в корне проекта, рядом с docker-compose.yml)

### Что НЕ нужно делать:
- ❌ Экспортировать переменные в shell хоста: `export OPENAI_API_KEY=...`
- ❌ Копировать `.env.prod` внутрь `soul_bot/`
- ❌ Создавать отдельный `.env` файл
- ❌ Использовать `make redeploy` (удаляет БД!)

### Что нужно сделать:
- ✅ Убедиться что `.env.prod` на месте
- ✅ Заполнить placeholder'ы реальными значениями:
  - `HELPER_ID=your_helper_assistant_id_here` → реальный ID
  - `RELATIONSHIPS_ID=your_relationships_assistant_id_here` → реальный ID
  - `SECRET_KEY=your_yookassa_secret_key_here` → реальный ключ
- ✅ Проверить что `POSTGRES_PASSWORD` не пустой

## 🔍 Проверка работы

```bash
# Проверить что контейнеры запущены
docker-compose ps

# Проверить что переменные загрузились
docker exec soulnear_bot env | grep -E "BOT_TOKEN|OPENAI_API_KEY"

# Проверить логи на ошибки
make logs-bot
```

## 📊 Результат

- ✅ Все переменные грузятся из `.env.prod`
- ✅ Нет warning'ов про missing variables
- ✅ Нет ошибки `'ContainerConfig'`
- ✅ БД не удаляется при редеплое
- ✅ Единая точка правды для конфигурации

---

**Красиво и работает! 🎉**

