# 🧪 Стратегия тестирования по этапам

**Цель:** После каждого этапа убедиться, что:
1. ✅ Новый функционал работает
2. ✅ Старый функционал не сломался
3. ✅ Качество ответов не ухудшилось

---

## 🛡️ Защитные механизмы

### 1. Feature Flags (Главное!)

Все новые фичи разрабатываются **за feature flags**, чтобы можно было:
- ✅ Включать/выключать одной переменной
- ✅ Откатиться мгновенно при проблеме
- ✅ Тестировать на тестовом боте, не трогая прод

**Пример в config.py:**
```python
# Feature Flags
FEATURE_FLAGS = {
    'USE_CHAT_COMPLETION': os.getenv('USE_CHAT_COMPLETION', 'false').lower() == 'true',
    'ENABLE_STYLE_SETTINGS': os.getenv('ENABLE_STYLE_SETTINGS', 'false').lower() == 'true',
    'ENABLE_USER_PROFILES': os.getenv('ENABLE_USER_PROFILES', 'false').lower() == 'true',
    'ENABLE_DYNAMIC_QUIZ': os.getenv('ENABLE_DYNAMIC_QUIZ', 'false').lower() == 'true',
    'ENABLE_PATTERN_ANALYSIS': os.getenv('ENABLE_PATTERN_ANALYSIS', 'false').lower() == 'true',
}
```

**В .env.test:**
```bash
# Включаем новые фичи только на тестовом боте
USE_CHAT_COMPLETION=true
ENABLE_STYLE_SETTINGS=true
```

**В .env.prod:**
```bash
# Продакшн - все выключено до полного тестирования
USE_CHAT_COMPLETION=false
ENABLE_STYLE_SETTINGS=false
```

---

### 2. Два параллельных бота

Всегда запущены **одновременно**:
- 🟢 **PROD бот** - стабильная версия для реальных пользователей
- 🔵 **TEST бот** - новые фичи для тестирования

**Запуск:**
```bash
# Terminal 1 - PROD (старая версия)
ENV=prod python bot.py

# Terminal 2 - TEST (новые фичи)
ENV=test python bot.py
```

**Преимущества:**
- Можешь сравнивать ответы в реальном времени
- Пользователи не страдают от багов
- Спокойно экспериментируешь на тесте

---

### 3. Regression Test Checklist

После каждого этапа проходим **ручной чеклист** на TEST боте:

#### ✅ **Базовые сценарии (ОБЯЗАТЕЛЬНО)**
- [ ] `/start` - показывает меню
- [ ] Выбор пола - сохраняется
- [ ] Выбор возраста - сохраняется
- [ ] `/helper` - отправляет сообщение → получает ответ
- [ ] `/soulsleep` - отправляет сообщение → получает ответ
- [ ] Квиз "Отношения" - проходит без ошибок
- [ ] Квиз "Деньги" - проходит без ошибок
- [ ] Квиз "Уверенность" - проходит без ошибок
- [ ] Квиз "Страхи" - проходит без ошибок
- [ ] Оплата - открывается форма
- [ ] `/menu` - возвращает в главное меню

#### ✅ **Новые фичи (специфично для этапа)**
Зависит от того, что добавили.

---

## 📋 Тестирование по этапам

### **ЭТАП 1: Миграция ChatCompletion API**

#### 🔧 Feature Flag
```python
FEATURE_FLAGS['USE_CHAT_COMPLETION'] = True  # только на TEST боте
```

#### ✅ Тесты
1. **Функциональные:**
   - [ ] `/helper` - ответ приходит через ChatCompletion
   - [ ] `/soulsleep` - ответ приходит через ChatCompletion
   - [ ] История сохраняется в БД
   - [ ] Контекст поддерживается (отправляешь 3 сообщения подряд, бот помнит предыдущие)
   - [ ] Токены считаются правильно

2. **Regression:**
   - [ ] Все базовые сценарии работают ✅
   - [ ] Время ответа ≤ старой версии

3. **Качество:**
   - [ ] Сравни 5 ответов TEST vs PROD на одинаковые вопросы
   - [ ] Новые ответы НЕ хуже старых (субъективно)

4. **Performance:**
   - [ ] Время ответа < 3 секунд
   - [ ] БД не тормозит (проверь через `\timing` в psql)

#### 🎯 Критерий завершения:
✅ TEST бот отвечает так же хорошо (или лучше), чем PROD  
✅ Все regression тесты зелёные  
✅ Feature flag работает (можно включить/выключить)

#### 📊 Rollout:
```bash
# Если всё ОК - включаем на проде
# .env.prod
USE_CHAT_COMPLETION=true
```

---

### **ЭТАП 2: Настройка стиля ответов**

#### 🔧 Feature Flag
```python
FEATURE_FLAGS['ENABLE_STYLE_SETTINGS'] = True  # только на TEST боте
```

#### ✅ Тесты
1. **Функциональные:**
   - [ ] `/settings` - открывается меню настроек
   - [ ] Выбор тона (формальный/дружеский/саркастичный/мотивирующий)
   - [ ] Выбор личности (наставник/друг/коуч)
   - [ ] Выбор длины (краткий/средний/развёрнутый)
   - [ ] Настройки сохраняются в БД
   - [ ] Ответы меняются в зависимости от настроек

2. **Regression:**
   - [ ] Все базовые сценарии работают ✅
   - [ ] Если настройки не установлены - работает дефолтный стиль

3. **Качество:**
   - [ ] Протестируй каждый стиль (4 тона × 3 личности × 3 длины = 36 комбинаций)
   - [ ] Хотя бы 5 основных комбинаций вручную:
     - Формальный + наставник + средний
     - Дружеский + друг + краткий
     - Саркастичный + друг + краткий
     - Мотивирующий + коуч + развёрнутый
     - Дефолтный (без настроек)

4. **Performance:**
   - [ ] Время ответа не увеличилось

#### 🎯 Критерий завершения:
✅ Стили реально отличаются друг от друга  
✅ Пользователь может легко настроить  
✅ Без настроек всё работает как раньше

---

### **ЭТАП 3: Профайл пользователя**

#### 🔧 Feature Flag
```python
FEATURE_FLAGS['ENABLE_USER_PROFILES'] = True
FEATURE_FLAGS['ENABLE_PATTERN_ANALYSIS'] = True
```

#### ✅ Тесты
1. **Функциональные:**
   - [ ] Профиль создаётся при первом сообщении
   - [ ] Паттерны анализируются после N сообщений
   - [ ] Профиль обновляется корректно
   - [ ] `/my_profile` - показывает профиль
   - [ ] Паттерны влияют на ответы

2. **Regression:**
   - [ ] Все базовые сценарии работают ✅
   - [ ] Если feature flag выключен - работает без профилей

3. **Качество:**
   - [ ] Отправь 10 сообщений на одну тему (например, "проблемы на работе")
   - [ ] Проверь, что паттерны выявлены корректно
   - [ ] Отправь сообщение на другую тему - бот учитывает предыдущие паттерны

4. **Performance:**
   - [ ] Анализ паттернов не блокирует ответ (асинхронный)
   - [ ] БД запросы оптимизированы

#### 🎯 Критерий завершения:
✅ Паттерны выявляются адекватно  
✅ Ответы становятся более персонализированными  
✅ Без заметного замедления

---

### **ЭТАП 4: Динамический квиз**

#### 🔧 Feature Flag
```python
FEATURE_FLAGS['ENABLE_DYNAMIC_QUIZ'] = True
```

#### ✅ Тесты
1. **Функциональные:**
   - [ ] Квиз "Отношения" - вопросы генерируются динамически
   - [ ] Вопросы учитывают предыдущие ответы
   - [ ] Вопросы учитывают профиль пользователя
   - [ ] Финальный анализ генерируется корректно
   - [ ] Инсайты сохраняются в профиль

2. **Regression:**
   - [ ] Если feature flag выключен - работает старый статический квиз
   - [ ] Все базовые сценарии работают ✅

3. **Качество:**
   - [ ] Пройди квиз 2 раза с разными ответами
   - [ ] Проверь, что вопросы отличаются
   - [ ] Финальный анализ релевантный

4. **Performance:**
   - [ ] Генерация вопроса < 5 секунд

#### 🎯 Критерий завершения:
✅ Вопросы действительно адаптивные  
✅ Финальный анализ глубже, чем раньше  
✅ Можно откатиться на статический квиз

---

### **ЭТАП 5: Прокаченные промпты**

#### 🔧 Изменения
Это не новая фича, а улучшение существующих промптов.

#### ✅ Тесты
1. **A/B тестирование:**
   - [ ] Создай 2 версии промпта (v1 = старый, v2 = новый)
   - [ ] Отправь 10 одинаковых вопросов в v1 и v2
   - [ ] Сравни качество ответов

2. **Regression:**
   - [ ] Все базовые сценарии работают ✅
   - [ ] Ответы не стали хуже

3. **Качество:**
   - [ ] Новые промпты дают более точные/релевантные ответы
   - [ ] Инсайты глубже
   - [ ] Меньше "воды" в ответах

#### 🎯 Критерий завершения:
✅ Новые промпты объективно лучше (по метрикам или субъективно)  
✅ Versioning работает (можно откатиться на старую версию)

---

### **ЭТАП 6: Файнтюнинг стиля**

#### 🔧 Feature Flag
```python
FEATURE_FLAGS['ENABLE_TUNE_STYLE'] = True
```

#### ✅ Тесты
1. **Функциональные:**
   - [ ] `/tune_style` - запускается диалог
   - [ ] Ответы пользователя сохраняются
   - [ ] Custom instructions генерируются
   - [ ] Стиль меняется после tune
   - [ ] Можно откатиться на дефолтный

2. **Regression:**
   - [ ] Все базовые сценарии работают ✅
   - [ ] Без `/tune_style` всё работает как раньше

3. **Качество:**
   - [ ] Пройди `/tune_style` и настрой стиль
   - [ ] Отправь 5 сообщений - проверь, что стиль изменился
   - [ ] Фидбек от 2-3 тестовых пользователей

#### 🎯 Критерий завершения:
✅ Стиль действительно меняется после tune  
✅ Пользователь доволен результатом

---

## 🔥 Автоматизация тестов

### Создадим smoke test suite:

**`tests/smoke_tests.py`:**
```python
"""
Smoke тесты для проверки базовой функциональности после каждого этапа.
Запуск: pytest tests/smoke_tests.py
"""
import pytest
import asyncio
from bot.loader import bot, dp
from database import db_user

class TestBasicFlow:
    """Базовый флоу - должен работать ВСЕГДА"""
    
    @pytest.mark.asyncio
    async def test_bot_starts(self):
        """Бот запускается без ошибок"""
        assert bot.token is not None
        assert dp is not None
    
    @pytest.mark.asyncio
    async def test_database_connection(self):
        """БД доступна"""
        user = await db_user.get_user(123456789)
        assert user is not None or user is None  # Просто проверяем, что запрос не падает
    
    @pytest.mark.asyncio
    async def test_openai_api(self):
        """OpenAI API доступен"""
        from bot.services.openai_service import get_chat_completion
        # TODO: реализовать после миграции

class TestRegressionFlow:
    """Критические сценарии - НЕ ДОЛЖНЫ СЛОМАТЬСЯ"""
    
    @pytest.mark.asyncio
    async def test_user_creation(self):
        """Создание пользователя работает"""
        # TODO: мокнуть Telegram update и протестировать
        pass
    
    @pytest.mark.asyncio
    async def test_helper_response(self):
        """Helper отвечает на сообщения"""
        # TODO: мокнуть запрос и проверить ответ
        pass
```

### Запуск после каждого этапа:
```bash
# Smoke тесты (быстрые, критичные)
pytest tests/smoke_tests.py -v

# Regression тесты (все тесты)
pytest tests/ -v

# Интеграционные тесты (медленные, полный флоу)
pytest tests/integration/ -v --slow
```

---

## 📊 Метрики качества

После каждого этапа собираем метрики:

### 1. **Технические метрики:**
- ⏱️ Время ответа (avg/p95/p99)
- 💾 Нагрузка на БД (queries per second)
- 🔥 Количество ошибок в логах
- 💰 Стоимость запросов к OpenAI

### 2. **Качественные метрики:**
- 👍 Субъективная оценка ответов (1-10)
- 📏 Длина ответов (соответствует настройкам?)
- 🎯 Релевантность (ответ по теме?)
- 🧠 Глубина инсайтов (есть ценность?)

### 3. **Пользовательские метрики:**
- 📈 Retention (возвращаются ли пользователи?)
- ⏱️ Session duration (дольше общаются?)
- ✅ Quiz completion rate (больше проходят?)
- ⚙️ Feature usage (используют ли настройки?)

---

## 🚀 Rollout процесс

### Чеклист перед включением на PROD:

1. ✅ **Все тесты зелёные:**
   ```bash
   pytest tests/ -v
   ```

2. ✅ **Ручной regression checklist пройден:**
   - Все базовые сценарии работают
   - Новая фича работает
   - Качество не хуже

3. ✅ **Логи чистые:**
   ```bash
   # Запускаем TEST бота на 1 час
   ENV=test python bot.py
   # Проверяем логи - нет ERROR/CRITICAL
   ```

4. ✅ **Performance в норме:**
   - Время ответа < 3 сек
   - БД не тормозит
   - Нет memory leaks

5. ✅ **Feature flag готов к включению:**
   ```bash
   # .env.prod
   NEW_FEATURE=true
   ```

6. ✅ **План отката:**
   - Feature flag можно выключить мгновенно
   - Известно, как откатить миграцию БД (если была)

### Процесс rollout:

1. **Включаем на PROD:**
   ```bash
   # .env.prod
   NEW_FEATURE=true
   ```

2. **Мониторим 1 час:**
   - Смотрим логи
   - Проверяем метрики
   - Читаем фидбек пользователей

3. **Если всё ОК:**
   - ✅ Feature стабильна
   - Переходим к следующему этапу

4. **Если есть проблемы:**
   - 🔴 Выключаем feature flag
   - 🔍 Анализируем проблему
   - 🔧 Фиксим на TEST боте
   - 🔁 Повторяем rollout

---

## 🎯 Итоговая стратегия

### После каждого этапа:

1. **Разработка:**
   - Код за feature flag
   - Unit тесты

2. **Тестирование на TEST боте:**
   - Smoke тесты (автоматические)
   - Regression checklist (ручной)
   - Качественная оценка

3. **Включение на PROD:**
   - Feature flag = true
   - Мониторинг 1 час
   - Решение: оставить или откатить

4. **Если стабильно:**
   - ✅ Переходим к следующему этапу
   - Feature flag становится дефолтным

---

## 🛠️ Инструменты

### Нужно создать:
- [x] Feature flags в config.py
- [ ] Smoke test suite
- [ ] Regression test checklist (Google Sheets?)
- [ ] Мониторинг метрик (простой logging + grep?)
- [ ] Rollout checklist

Готов начать с добавления feature flags? 😎

