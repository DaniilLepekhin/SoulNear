# 🐳 Docker Setup - Полная документация

## 📦 Что было создано

### Основные файлы

1. **`docker-compose.yml`** - основной файл композиции
   - PostgreSQL 15
   - Telegram Bot (soul_bot)
   - WebApp API (webapp_api)
   - Healthchecks
   - Networks и volumes

2. **`soul_bot/Dockerfile`** - образ для Telegram бота
   - Python 3.11
   - ffmpeg для аудио
   - Все зависимости из requirements.txt

3. **`webapp_api/Dockerfile`** - образ для Quart API
   - Python 3.11
   - Quart и зависимости

4. **`.dockerignore`** - игнорируемые файлы при сборке
   - venv, __pycache__, logs, .env и т.д.

### Конфигурация

5. **`.env.example`** - шаблон конфигурации
   - Все необходимые переменные окружения
   - Комментарии и примеры

6. **`validate-env.sh`** - скрипт валидации конфига
   - Проверяет обязательные переменные
   - Предупреждает о placeholder'ах

### Управление

7. **`Makefile`** - удобные команды для управления
   - `make up/down/restart/logs`
   - `make backup/restore`
   - `make health/stats`
   - `make dev` для разработки

8. **`docker-compose.dev.yml`** - конфиг для разработки
   - Volume mounts для hot reload
   - Debug режим

### Документация

9. **`README_DOCKER.md`** - подробная документация
10. **`DOCKER_QUICKSTART.md`** - быстрый старт
11. **`DOCKER_CHECKLIST.md`** - чек-лист запуска
12. **`DOCKER_SETUP_SUMMARY.md`** - этот файл

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────┐
│                  Docker Host                    │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐            │
│  │   soul_bot   │  │  webapp_api  │            │
│  │              │  │              │            │
│  │  aiogram 🤖  │  │   Quart 🌐   │            │
│  │              │  │   :8888      │            │
│  └──────┬───────┘  └──────┬───────┘            │
│         │                 │                     │
│         └────────┬────────┘                     │
│                  │                              │
│         ┌────────▼─────────┐                    │
│         │   PostgreSQL 15  │                    │
│         │     :5432        │                    │
│         └──────────────────┘                    │
│                                                 │
│  Network: soulnear_network (bridge)             │
│  Volume: postgres_data (persistent)             │
└─────────────────────────────────────────────────┘
```

## 🚀 Быстрый старт

```bash
# 1. Создать конфиг
make setup

# 2. Заполнить .env.prod
nano .env.prod

# 3. Запустить
make up

# 4. Проверить
make logs
make health
```

## 📋 Основные команды

### Управление жизненным циклом

```bash
make up          # Запустить все
make down        # Остановить все
make restart     # Перезапустить все
make rebuild     # Пересобрать и запустить
make ps          # Статус сервисов
make stats       # Использование ресурсов
```

### Логи

```bash
make logs        # Все логи
make logs-bot    # Логи бота
make logs-api    # Логи API
make logs-db     # Логи PostgreSQL
```

### База данных

```bash
make backup      # Создать бэкап
make restore     # Восстановить бэкап
make shell-db    # Подключиться к psql
```

### Отладка

```bash
make shell-bot   # Shell внутри контейнера бота
make shell-api   # Shell внутри контейнера API
make health      # Проверить здоровье всех сервисов
```

### Разработка

```bash
make dev         # Запустить с hot reload
make dev-build   # Пересобрать и запустить dev
make dev-down    # Остановить dev режим
```

## 🔐 Безопасность

### Обязательно

- ✅ `.env.prod`, `.env.test`, `.env.dev` в `.gitignore`
- ✅ Используйте сильные пароли для `POSTGRES_PASSWORD`
- ✅ Не коммитьте API ключи
- ✅ Бэкапы БД делайте регулярно

### Рекомендуется

- 🔒 В production используйте Docker secrets
- 🔒 Настройте firewall правила
- 🔒 Ограничьте ресурсы контейнеров
- 🔒 Включите SSL для API (через nginx/traefik)
- 🔒 Настройте мониторинг и алерты

## 🎯 Окружения

Проект поддерживает 3 окружения:

### Production (`ENV=prod`)

```bash
ENV=prod make up
```

- Используется `.env.prod`
- Оптимизированные образы
- Restart: unless-stopped
- Логирование минимальное

### Test (`ENV=test`)

```bash
ENV=test make up
```

- Используется `.env.test`
- Отдельная тестовая БД
- Можно безопасно ломать

### Development (`ENV=dev`)

```bash
make dev
```

- Используется `.env.dev`
- Hot reload кода
- Debug режим
- Volume mounts для live editing

## 📊 Мониторинг

### Healthchecks

PostgreSQL:
```bash
docker exec soulnear_postgres pg_isready -U postgres
```

API:
```bash
curl http://localhost:8888/health
```

### Логи

```bash
# Real-time логи
docker-compose logs -f --tail=100

# Логи за последний час
docker-compose logs --since 1h

# Только ошибки
docker-compose logs | grep ERROR
```

### Ресурсы

```bash
# Текущее использование
docker stats --no-stream

# Использование дискового пространства
docker system df
```

## 🔧 Troubleshooting

### Проблема: Бот не подключается к БД

**Причины:**
- PostgreSQL не запущен
- Неправильные credentials
- Сеть не настроена

**Решение:**
```bash
# Проверить статус PostgreSQL
docker-compose ps postgres

# Проверить логи
make logs-db

# Проверить переменные окружения
docker exec soulnear_bot env | grep POSTGRES

# Перезапустить
make restart
```

### Проблема: API не отвечает

**Причины:**
- Порт занят
- Ошибка в коде
- Не подключается к БД

**Решение:**
```bash
# Проверить порт
lsof -i :8888

# Проверить логи
make logs-api

# Проверить health
curl http://localhost:8888/health

# Перезапустить
make restart-api
```

### Проблема: База данных не сохраняется

**Причины:**
- Volume не примонтирован
- Используется `docker-compose down -v`

**Решение:**
```bash
# Проверить volumes
docker volume ls | grep soul

# Инспектировать volume
docker volume inspect soulnear_postgres_data

# При остановке НЕ используйте -v
make down  # ✅ OK
# docker-compose down -v  # ❌ УДАЛИТ БД!
```

### Проблема: Out of memory

**Решение:**
```bash
# Добавьте в docker-compose.yml:
services:
  bot:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
```

### Проблема: Долгая сборка образов

**Решение:**
```bash
# Используйте build cache
docker-compose build --parallel

# Или скачайте готовые образы (если настроен registry)
docker-compose pull
```

## 🎓 Best Practices

### Development

1. **Используйте `make dev`** для разработки с hot reload
2. **Коммитьте `.env.example`**, но не `.env.*`
3. **Регулярно обновляйте зависимости** в requirements.txt
4. **Тестируйте изменения** в test окружении

### Production

1. **Регулярные бэкапы** (настройте cron):
   ```bash
   0 3 * * * cd /path/to/SoulNear && make backup
   ```

2. **Мониторинг логов**:
   ```bash
   # Настройте log rotation
   docker-compose logs --tail=1000 > /var/log/soulnear.log
   ```

3. **Обновления безопасности**:
   ```bash
   # Обновить base images
   docker-compose pull
   docker-compose up -d --build
   ```

4. **Health checks**:
   ```bash
   # Проверяйте здоровье регулярно
   */5 * * * * cd /path/to/SoulNear && make health
   ```

### CI/CD Integration

```yaml
# .github/workflows/docker.yml
name: Docker Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build images
        run: docker-compose build
      - name: Run tests
        run: docker-compose run bot pytest
```

## 📚 Дополнительные ресурсы

- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [PostgreSQL Docker Image](https://hub.docker.com/_/postgres)
- [Python Docker Best Practices](https://docs.docker.com/language/python/best-practices/)

## 🆘 Получить помощь

1. Проверьте логи: `make logs`
2. Проверьте health: `make health`
3. Посмотрите checklist: `DOCKER_CHECKLIST.md`
4. Почитайте troubleshooting выше
5. Проверьте переменные окружения: `./validate-env.sh`

---

**Сделано с ❤️ и Docker магией ✨**

_P.S. Если что-то не работает - сначала проверь .env файл, в 90% случаев проблема там_ 😉

