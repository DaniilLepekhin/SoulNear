# 🚀 Рабочий процесс разработки

**Краткая шпаргалка:** Как разрабатывать новые фичи без страха сломать прод

---

## 📐 Архитектура безопасности

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  🟢 PROD бот                    🔵 TEST бот                 │
│  (стабильная версия)            (новые фичи)                │
│                                                             │
│  ├─ .env.prod                   ├─ .env.test                │
│  ├─ Feature flags: OFF          ├─ Feature flags: ON        │
│  └─ Реальные пользователи       └─ Тестирование             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Главный принцип:** Все новые фичи разрабатываются **за feature flags**, которые выключены на проде по умолчанию.

---

## 🔄 Цикл разработки этапа

### 1️⃣ Разработка (на TEST боте)

```bash
# Включаем feature flag в .env.test
echo "USE_CHAT_COMPLETION=true" >> soul_bot/.env.test

# Пишем код
# Добавляем проверку feature flag:
from config import is_feature_enabled

if is_feature_enabled('USE_CHAT_COMPLETION'):
    # Новая логика
    result = new_openai_service.get_completion(...)
else:
    # Старая логика (fallback)
    result = old_chatgpt.answer(...)
```

### 2️⃣ Тестирование

```bash
# Запускаем автоматические тесты
cd scripts
./test_stage.sh 1

# Выйдет:
# ✅ Smoke тесты
# 📋 Ручной regression checklist
# 🚩 Статус feature flags
```

**Ручной чеклист:**
1. Открываешь `soul_bot/REGRESSION_CHECKLIST.md`
2. Запускаешь TEST бота: `ENV=test python bot.py`
3. Проходишь по всем пунктам
4. Отмечаешь ✅ или ❌

### 3️⃣ Сравнение TEST vs PROD

Запускаешь **оба бота одновременно**:

```bash
# Terminal 1 - PROD (старая версия)
cd soul_bot && ENV=prod python bot.py

# Terminal 2 - TEST (новая версия)
cd soul_bot && ENV=test python bot.py
```

**Отправляешь одинаковые сообщения в оба бота:**
- "Я чувствую себя одиноко"
- "Как мне улучшить отношения?"
- "У меня проблемы с деньгами"

**Оцениваешь:**
- ✅ TEST лучше или равно PROD → можно на прод
- ❌ TEST хуже PROD → нужно доработать

### 4️⃣ Rollout на PROD

**Если всё ОК:**

```bash
# 1. Включаешь feature flag на проде
echo "USE_CHAT_COMPLETION=true" >> soul_bot/.env.prod

# 2. Перезапускаешь PROD бота
cd soul_bot && ENV=prod python bot.py

# 3. Мониторишь 1 час
# - Смотришь логи (нет ERROR?)
# - Проверяешь метрики (время ответа?)
# - Читаешь фидбек пользователей

# 4. Если проблемы - откатываешь
echo "USE_CHAT_COMPLETION=false" >> soul_bot/.env.prod
# Перезапускаешь бота
```

**План отката всегда готов!** ⚡

---

## 🛡️ 3 уровня защиты

### Уровень 1: Feature Flags ⚡ (мгновенный откат)

```python
# config.py
FEATURE_FLAGS = {
    'USE_CHAT_COMPLETION': os.getenv('USE_CHAT_COMPLETION', 'false').lower() == 'true',
    # ... другие фичи
}

# В коде
from config import is_feature_enabled

if is_feature_enabled('USE_CHAT_COMPLETION'):
    # Новая логика
else:
    # Старая логика (всегда работает!)
```

**Преимущества:**
- ✅ Откат за 1 секунду (меняешь .env и перезапускаешь)
- ✅ Старая логика всегда доступна
- ✅ Можно A/B тестировать

### Уровень 2: Автоматические тесты 🤖 (быстрая проверка)

```bash
# Smoke тесты (2-3 минуты)
cd soul_bot
python tests/smoke_tests.py

# Проверяют:
# ✅ Бот запускается
# ✅ БД доступна
# ✅ Критические handlers существуют
# ✅ OpenAI API работает
```

**Преимущества:**
- ✅ Быстро (2-3 минуты)
- ✅ Автоматически (не нужно думать)
- ✅ Ловит очевидные баги

### Уровень 3: Ручное тестирование 👤 (глубокая проверка)

```bash
# Regression checklist
open soul_bot/REGRESSION_CHECKLIST.md

# Проходишь вручную:
# ✅ /start
# ✅ /helper
# ✅ Квизы
# ✅ Оплата
# ✅ Сравнение качества ответов
```

**Преимущества:**
- ✅ Проверяешь реальный UX
- ✅ Оцениваешь качество ответов
- ✅ Находишь тонкие баги

---

## 📊 Метрики качества

После каждого этапа собирай метрики:

### Технические:
```bash
# Время ответа
# Смотри в логах:
INFO:aiogram.event:Update id=123 is handled. Duration 150 ms

# Цель: < 3000 ms
```

### Качественные:
```
Оценка ответов (1-10):
- Релевантность: ___ / 10
- Глубина: ___ / 10
- Полезность: ___ / 10
- Стиль: ___ / 10

Средняя оценка: ___ / 10
```

### Пользовательские:
```
- Сколько пользователей используют фичу? ___
- Сколько проходят квизы до конца? ___
- Сколько возвращаются на следующий день? ___
```

---

## 🔥 Быстрые команды

### Запуск ботов:
```bash
# PROD
cd soul_bot && ENV=prod python bot.py

# TEST
cd soul_bot && ENV=test python bot.py
```

### Тестирование:
```bash
# Автоматические тесты
cd soul_bot && python tests/smoke_tests.py

# Комплексное тестирование этапа
cd scripts && ./test_stage.sh 1
```

### Проверка конфига:
```bash
# Показывает все настройки и feature flags
cd soul_bot && python config.py
```

### Проверка БД:
```bash
# Подключись к БД
psql -U nikitagorokhov -d soul_bot

# Полезные запросы
SELECT COUNT(*) FROM users;
SELECT * FROM user_profiles LIMIT 5;
SELECT * FROM conversation_history ORDER BY timestamp DESC LIMIT 10;
```

### Мониторинг логов:
```bash
# В реальном времени
cd soul_bot && ENV=prod python bot.py | grep ERROR

# Только критичные ошибки
cd soul_bot && ENV=prod python bot.py 2>&1 | grep -E "ERROR|CRITICAL"
```

---

## ✅ Чеклист перед rollout

- [ ] ✅ Smoke тесты зелёные
- [ ] ✅ Regression checklist пройден
- [ ] ✅ Качество TEST ≥ PROD
- [ ] ✅ Логи чистые (нет ERROR)
- [ ] ✅ Performance в норме (< 3 сек)
- [ ] ✅ Feature flag работает
- [ ] ✅ План отката готов

**Если всё ✅ → делай rollout!**

---

## 🚨 Что делать при проблемах

### Проблема: Тесты провалились
```bash
# Смотри детали ошибки
cd soul_bot && python tests/smoke_tests.py -v

# Фиксишь код
# Перезапускаешь тесты
```

### Проблема: TEST бот хуже PROD
```
1. Анализируй причину (почему хуже?)
2. Улучшай промпты / логику
3. Тестируешь снова
4. Повторяешь, пока не станет лучше
```

### Проблема: Ошибки в логах
```bash
# Смотри traceback
# Фиксишь код
# Перезапускаешь TEST бота
ENV=test python bot.py
```

### Проблема: Prod сломался после rollout
```bash
# МГНОВЕННЫЙ ОТКАТ!
# 1. Выключи feature flag
echo "NEW_FEATURE=false" >> soul_bot/.env.prod

# 2. Перезапусти
cd soul_bot && ENV=prod python bot.py

# 3. Анализируй проблему на TEST
cd soul_bot && ENV=test python bot.py

# 4. Фиксишь → тестируешь → повторяешь rollout
```

---

## 🎯 Итоговый workflow

```
1. Разработка
   ├─ Код за feature flag
   ├─ Тесты
   └─ Запуск на TEST боте

2. Тестирование
   ├─ Автоматические тесты (./test_stage.sh)
   ├─ Ручной checklist
   └─ Сравнение TEST vs PROD

3. Rollout
   ├─ Включение feature flag на PROD
   ├─ Мониторинг 1 час
   └─ Решение: оставить / откатить

4. Стабилизация
   ├─ Если ОК → переход к следующему этапу
   └─ Если проблемы → фикс на TEST и повтор
```

---

## 📚 Документация

- `IMPLEMENTATION_ROADMAP.md` - полный план разработки
- `TESTING_STRATEGY.md` - подробная стратегия тестирования
- `REGRESSION_CHECKLIST.md` - ручной чеклист тестирования
- `WORKFLOW_SUMMARY.md` - эта шпаргалка

---

**Принцип:** Лучше потратить 1 час на тестирование, чем 5 часов на поиск бага на проде. 🧠

**Результат:** Спокойная разработка без страха сломать прод. 😎

