# ⚡ Quick Start: Рефакторинг за 10 минут

## TL;DR

Сейчас: Две папки с дубликатом кода ❌  
Станет: Одна папка, разные конфиги ✅

---

## Шаг 1: Бэкап (1 мин)

```bash
cd /Users/nikitagorokhov/dev/SoulNear

# Создаём резервные копии (на всякий случай)
cp -r soul_bot soul_bot_backup
cp -r soul_test_bot soul_test_bot_backup
```

---

## Шаг 2: Создаём .env файлы (2 мин)

### .env.prod (продакшн)
```bash
cd soul_bot
cp .env.example .env.prod
```

Открой `.env.prod` и заполни:
```env
ENV=prod
BOT_TOKEN=<твой продакшн токен из soul_bot/config.py>
TEST=false
POSTGRES_DB=soul_bot
OPENAI_API_KEY=<твой ключ>
HELPER_ID=<твой ассистент>
# ... остальное
```

### .env.test (тестовый)
```bash
cp .env.example .env.test
```

Открой `.env.test` и заполни:
```env
ENV=test
BOT_TOKEN=7838929567:AAELlItbyGN8KvzeYdY-28id6Fhtf6Zn0PY
TEST=true
POSTGRES_DB=soul_test_bot
OPENAI_API_KEY=<твой ключ>
# ... остальное
```

---

## Шаг 3: Обновляем config.py (2 мин)

```bash
# Заменяем старый config.py на новый
cd soul_bot
mv config.py config_old.py
mv config_refactored.py config.py
```

**Готово!** Новый config.py автоматически подхватывает .env файлы.

---

## Шаг 4: Тестируем (3 мин)

### Тестовый бот:
```bash
cd /Users/nikitagorokhov/dev/SoulNear
./scripts/run_test.sh
```

Зайди в Telegram, отправь сообщение тестовому боту. Должно работать!

### Продакшн бот:
```bash
cd /Users/nikitagorokhov/dev/SoulNear
./scripts/run_prod.sh
```

Проверь, что продакшн бот тоже работает.

---

## Шаг 5: Копируем уникальные файлы (1 мин)

```bash
# Проверяем, есть ли уникальные файлы в soul_test_bot
cd /Users/nikitagorokhov/dev/SoulNear
diff -rq soul_bot soul_test_bot | grep "Only in soul_test_bot"

# Если нашли что-то важное — копируем
# Например:
# cp soul_test_bot/bot/handlers/user/webapp.py soul_bot/bot/handlers/user/
```

---

## Шаг 6: Удаляем дубликат (1 мин)

**Только когда убедился, что всё работает!**

```bash
cd /Users/nikitagorokhov/dev/SoulNear

# Удаляем soul_test_bot
rm -rf soul_test_bot

# Коммитим изменения
cd soul_bot
git add .
git commit -m "refactor: объединил soul_bot и soul_test_bot, использую .env файлы"
git push
```

---

## Готово! 🎉

Теперь у тебя:
✅ Одна кодовая база  
✅ Безопасные .env файлы  
✅ Удобные скрипты запуска  
✅ Чистый git history  

---

## Запуск теперь такой:

```bash
# Продакшн
./scripts/run_prod.sh

# Тест
./scripts/run_test.sh

# Или напрямую:
cd soul_bot
ENV=prod python bot.py   # продакшн
ENV=test python bot.py   # тест
```

---

## Откат (если что-то пошло не так)

```bash
# Восстанавливаем из бэкапа
rm -rf soul_bot
cp -r soul_bot_backup soul_bot
```

---

## FAQ

**Q: А если я забыл какой-то токен в .env файле?**  
A: Бот упадёт с понятной ошибкой: `BOT_TOKEN не найден в .env.prod`

**Q: Можно ли запускать оба бота одновременно?**  
A: Да! В разных терминалах:
```bash
# Терминал 1
./scripts/run_prod.sh

# Терминал 2
./scripts/run_test.sh
```

**Q: Где хранить .env.prod и .env.test?**  
A: Они в `.gitignore`, НЕ коммитятся в git. Храни локально или в password manager.

**Q: А soul_bot_backup можно удалить?**  
A: Через неделю, когда точно убедишься, что всё работает.


