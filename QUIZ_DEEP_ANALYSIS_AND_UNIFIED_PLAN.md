# 🧠 Глубокий анализ квизов SoulNear + Объединённый план улучшений

**Дата анализа:** 1 ноября 2025  
**Автор:** AI Agent (Claude Sonnet 4.5)  
**Задача:** Проанализировать 2 плана от агентов, текущую кодовую базу, выявить что реализовано, что нет, и составить оптимальный объединённый план

---

## 📋 EXECUTIVE SUMMARY

### Текущее состояние квизов (что РЕАЛЬНО работает)

✅ **Реализовано и работает:**
1. Real-time dialogue (каждый вопрос генерируется адаптивно на основе предыдущих ответов)
2. Детектирование противоречий (keyword-based + branch questions)
3. Mid-quiz инсайты (после Q3 и Q6)
4. Hard-code ключевых полей (`contradiction`, `hidden_dynamic`, `blocked_resource`)
5. GPT-4o для финального анализа
6. Confidence stars (⭐⭐⭐⭐⭐)
7. Категории под заказчика: relationships/money/purpose
8. Адаптивные вопросы (8 базовых + до 3 follow-up = 10-11 total)
9. Интеграция с профилем пользователя
10. Поддержка голосовых ответов

✅ **Теоретически реализовано (код есть, но не работает):**
- Разные типы вопросов: `text`, `scale`, `multiple_choice`
- **ПРОБЛЕМА:** GPT генерирует только `text` и `open` типы

❌ **НЕ реализовано из requirements:**
1. GPT-based детектирование противоречий (сейчас keyword-based heuristics)
2. Conversational tone в промптах (сейчас частично)
3. Финальный вывод с подсветкой скрытой динамики (частично реализовано, но не идеально)
4. Стилистика "честного друга" (требует переписывания всех промптов)

---

## 🔍 АНАЛИЗ ДВУХ ПЛАНОВ АГЕНТОВ

### План 1 (Компиляция Агентов 1, 3, 4)

**Философия:** "90% пользы при 20% усилий" - фокус на Quick Wins

**Tier 1 (КРИТИЧНО):**
| # | Что | Реализовано? | Оценка реализации |
|---|-----|--------------|-------------------|
| 1 | Real-time dialogue | ✅ **ДА** | 100% (уже работает) |
| 2 | Conversational prompts | ⚠️ **ЧАСТИЧНО** | 40% (нужна переработка) |
| 3 | GPT contradiction detection | ❌ **НЕТ** | 0% (keyword-based heuristics) |
| 4 | Hard-code key fields | ✅ **ДА** | 100% (уже работает) |
| 5 | Synthesis patterns | ⚠️ **ЧАСТИЧНО** | 50% (есть, но не оптимально) |
| 6 | Fix categories | ✅ **ДА** | 100% (relationships/money/purpose) |

**Сильные стороны плана:**
- ✅ Фокус на глубину, а не поверхность
- ✅ Учёт новых пользователей (seed questions)
- ✅ Реалистичные оценки стоимости ($0.084/quiz vs $0.034)
- ✅ Приоритизация по ROI

**Слабости:**
- ❌ Не учитывает проблему типов вопросов
- ❌ Переоценивает сложность того, что уже реализовано
- ⚠️ Слишком технический язык, мало примеров

---

### План 2 (Компиляция других агентов)

**Философия:** "Без соплей" - прагматичный подход с фокусом на результат

**Топ-10 улучшений:**
| # | Что | Реализовано? | ROI | Сложность |
|---|-----|--------------|-----|-----------|
| 1 | Hard-code финального вывода | ✅ **ДА** | 🔥 | S |
| 2 | Разговорные промпты | ⚠️ **ЧАСТИЧНО** | 🔥 | S |
| 3 | Генерация после каждого ответа | ✅ **ДА** | 🔥 | M |
| 4 | GPT противоречия | ❌ **НЕТ** | 🔥 | M |
| 5 | Стартовые крючки | ✅ **ДА** | 🔥 | S |
| 6 | Mid-quiz подсветка | ✅ **ДА** | 👍 | S-M |
| 7 | UI "не тест, а разговор" | ⚠️ **ЧАСТИЧНО** | 👍 | S |
| 8 | Расширение типов вопросов | ⚠️ **ЧАСТИЧНО** | 👍 | M |
| 9 | Категории под заказчика | ✅ **ДА** | 👍 | S |
| 10 | Персональные рекомендации | ✅ **ДА** | 👍 | S |

**Сильные стороны:**
- ✅ Короткие ёмкие описания
- ✅ Честная оценка ROI vs complexity
- ✅ Фокус на новых пользователей
- ✅ Практичные следующие шаги

**Слабости:**
- ❌ Не учитывает что уже реализовано (7 из 10 пунктов уже есть!)
- ❌ Нет деталей по реализации
- ⚠️ Слишком амбициозный тайминг (1-2 дня на всё)

---

## 🎯 МОЙ ВЕРДИКТ ПО ПЛАНАМ

### Что агенты НЕ учли:

1. **Оба плана переоценивают объём работы**
   - Real-time dialogue уже работает ✅
   - Hard-code key fields уже работает ✅
   - Mid-quiz insights уже работают ✅
   - Категории уже соответствуют requirements ✅

2. **Оба плана недооценивают проблему типов вопросов**
   - Код поддерживает `scale` и `multiple_choice`
   - НО GPT генерирует только `text`/`open`
   - Это критично для UX (сейчас квиз = "сложный")

3. **Оба плана не упоминают голосовые ответы**
   - Уже реализованы ✅
   - Это огромное преимущество для UX!

4. **Оба плана игнорируют существующие mid-quiz инсайты**
   - Уже работают после Q3 и Q6 ✅
   - Это именно то, что просил заказчик!

---

## 🔥 ЧТО РЕАЛЬНО НУЖНО СДЕЛАТЬ

### ПРОБЛЕМА №1: Типы вопросов (КРИТИЧНО!)

**Текущая ситуация:**
- Код поддерживает 3 типа: `text`, `scale`, `multiple_choice`
- GPT промпты просят генерировать разные типы
- НО на выходе только `text` и `open`

**Почему это происходит?**

Анализирую промпт в `generate_adaptive_question`:
```python
RETURN JSON (single question):
{
  "id": "q{question_number}",
  "text": "Question text",
  "type": "scale|choice|open",  # ← Здесь "choice" вместо "multiple_choice"!
  "options": [...],
  ...
}
```

И в `_generate_dynamic_batch`:
```python
Авторизованные типы: text (открытый ответ), multiple_choice (3-4 варианта), scale (5 вариантов)
```

**Проблема:** Промпты противоречат друг другу!
- `generate_adaptive_question` просит `"scale|choice|open"`
- `_generate_dynamic_batch` просит `"text|multiple_choice|scale"`
- GPT путается и генерирует только `open` (самый простой)

**РЕШЕНИЕ:**
1. Стандартизировать типы во ВСЕХ промптах: `text`, `scale`, `multiple_choice`
2. Добавить примеры КАЖДОГО типа в промпт
3. Добавить инструкцию: "IMPORTANT: Mix question types! Use scale for frequency, multiple_choice for binary decisions, text for deep exploration."

---

### ПРОБЛЕМА №2: GPT-based противоречия (ВАЖНО)

**Текущая ситуация:**
- Детектирование через keyword-based heuristics (`_detect_answer_contradictions`)
- Работает, но ловит только очевидное

**Из requirements заказчика:**
> "Переписать промты так, чтобы вопросы и логика анализа вели к неочевидным связям (например, через противоречия, скрытые мотивации, повторяющиеся реакции)."

**Что уже есть:**
- Keywords: "many friends" + "lonely" → contradiction
- Keywords: "balance" + "12 hours" → burnout denial

**Что нужно:**
- GPT анализирует все ответы семантически
- Находит неочевидные противоречия
- Пример: "Говоришь что любишь людей, но все хобби — одиночные"

**РЕШЕНИЕ:**
- Заменить keyword heuristics на GPT-анализ после каждых 2-3 ответов
- Prompt: "Find SUBTLE contradictions between answers. Not obvious (many friends + lonely), but hidden patterns."

---

### ПРОБЛЕМА №3: Conversational tone (ВАЖНО)

**Из requirements:**
> "Стилистика - живая, простая, без абстрактных терминов, как будто собеседник честно говорит «смотри, вот тут ты застрял, а вот здесь видно, чего ты боишься»."

**Текущие промпты:**
- ❌ "Analyze these quiz answers and identify psychological patterns"
- ❌ "Return JSON with patterns array"
- ⚠️ "Разговаривай как близкий друг" (есть, но слабо)

**Что нужно:**
- ✅ "Ты мой друг, который знает меня 10 лет и видит то, что я прячу от себя"
- ✅ "Говори прямо: 'Смотри, ты сам себе врёшь тут'"
- ✅ "Без канцелярита: не 'низкая самоэффективность', а 'ты не веришь что справишься'"

**РЕШЕНИЕ:**
1. Переписать ВСЕ system prompts в разговорном стиле
2. Добавить примеры "плохо vs хорошо"
3. Инструкция: "Don't sound like a therapist. Sound like a friend who calls you on your bullshit."

---

### ПРОБЛЕМА №4: Финальный вывод (СРЕДНЕ)

**Из requirements:**
> "Финальный вывод должен быть не «ты тревожный», а разбор паттерна с подсветкой:
> - какая скрытая динамика стоит за поведением,
> - где человек себя обманывает или блокирует,
> - какой ресурс в этом скрыт."

**Что уже есть (analyzer.py):**
```python
patterns = [
  {
    "contradiction": "...",  # ✅ Есть!
    "hidden_dynamic": "...", # ✅ Есть!
    "blocked_resource": "..." # ✅ Есть!
  }
]
```

**ПРОБЛЕМА:** Промпт частично реализует это, но финальное форматирование может быть лучше.

**РЕШЕНИЕ:**
- Обновить `format_results_for_telegram` для более разговорного стиля
- Добавить секцию "Что дальше?" с конкретными шагами

---

## 🎯 ОБЪЕДИНЁННЫЙ ПЛАН (ЛУЧШЕЕ ИЗ ДВУХ)

### Принципы объединения:

1. **Из Плана 1 берём:**
   - Фокус на глубину анализа
   - Учёт новых пользователей
   - Честные оценки стоимости

2. **Из Плана 2 берём:**
   - Короткий формат (таблица ROI/Complexity)
   - Прагматичный подход
   - Фокус на quick wins

3. **Добавляем от себя:**
   - Честную оценку что УЖЕ реализовано
   - Фокус на РЕАЛЬНЫХ проблемах (типы вопросов!)
   - Пошаговый план с приоритетами

---

## 📊 TIER 1: КРИТИЧНЫЕ УЛУЧШЕНИЯ (1-2 дня)

### ✅ 1. Исправить типы вопросов (ПРИОРИТЕТ #1)

**Проблема:** GPT генерирует только text вопросы
**Польза:** 10/10 - квиз станет интереснее, меньше усталости
**Сложность:** 2/10 - просто переписать промпты
**ROI:** 🔥🔥🔥

**Что делать:**
1. Стандартизировать типы в промптах:
   ```python
   "type": "text|scale|multiple_choice"
   ```

2. Добавить в промпт примеры:
   ```
   EXAMPLES:
   
   ❌ BAD (all text):
   Q1: "Расскажите о своих отношениях" (type: text)
   Q2: "Опишите свои страхи" (type: text)
   Q3: "Что вас беспокоит?" (type: text)
   
   ✅ GOOD (mixed types):
   Q1: "Расскажите о своих отношениях" (type: text)
   Q2: "Как часто вы чувствуете одиночество?" (type: scale, options: [Никогда, Редко, Иногда, Часто, Постоянно])
   Q3: "У вас больше друзей или знакомых?" (type: multiple_choice, options: [Больше друзей, Больше знакомых, Поровну])
   ```

3. Добавить инструкцию в `generate_adaptive_question`:
   ```
   IMPORTANT: Mix question types!
   - Use "text" for deep exploration (20-30% of questions)
   - Use "scale" for frequency/intensity (40-50%)
   - Use "multiple_choice" for binary/3-way decisions (20-30%)
   
   AVOID asking 5+ text questions in a row (user fatigue!)
   ```

4. Добавить post-processing валидацию:
   ```python
   # После генерации вопроса
   if len([q for q in last_3_questions if q['type'] == 'text']) >= 3:
       # Если последние 3 вопроса = text, force scale/multiple_choice
       question['type'] = random.choice(['scale', 'multiple_choice'])
   ```

**Файлы для изменения:**
- `soul_bot/bot/services/quiz_service/generator.py`
  - Строки 169-208 (`generate_adaptive_question` prompt)
  - Строки 600-635 (`_generate_dynamic_batch` prompt)

**Тест:**
- Запустить квиз
- Проверить что появляются scale и multiple_choice вопросы
- Ratio должен быть примерно: 30% text, 50% scale, 20% multiple_choice

---

### ✅ 2. GPT-based детектирование противоречий

**Проблема:** Keyword heuristics ловят только очевидное
**Польза:** 9/10 - найдёт скрытые паттерны, о которых пользователь не знает
**Сложность:** 4/10 - нужен GPT call после каждых 2-3 ответов
**ROI:** 🔥🔥

**Что делать:**

1. Создать новую функцию `_detect_contradictions_via_gpt`:
   ```python
   async def _detect_contradictions_via_gpt(
       answers: list[dict],
       category: str
   ) -> list[str]:
       """
       Использовать GPT для детектирования СКРЫТЫХ противоречий
       
       Не keyword matching, а семантический анализ:
       - "Говоришь что любишь людей, но все хобби одиночные"
       - "Хочешь стабильности, но меняешь работу каждый год"
       """
       
       answers_text = "\n".join([
           f"Q: {a['question_text']}\nA: {a['answer_value']}"
           for a in answers
       ])
       
       prompt = f"""
       You are a psychological DETECTIVE finding HIDDEN contradictions.
       
       CATEGORY: {category}
       
       ANSWERS:
       {answers_text}
       
       TASK: Find SUBTLE contradictions (not obvious ones).
       
       ❌ OBVIOUS (boring):
       "Says has many friends + feels lonely" (user already knows!)
       
       ✅ SUBTLE (insightful):
       "Says values relationships deeply + all hobbies are solitary"
       "Wants stability + changes jobs every year without external reason"
       "Claims to be open + avoids vulnerability in every example"
       
       Return JSON:
       {{
         "contradictions": [
           {{
             "summary": "Short description",
             "evidence": ["Quote from answer 1", "Quote from answer 2"],
             "insight": "What this REALLY means"
           }}
         ]
       }}
       
       Find 1-3 contradictions. If none found, return empty array.
       """
       
       response = await client.chat.completions.create(
           model="gpt-4o-mini",  # Быстро и дёшево для mid-quiz
           messages=[
               {"role": "system", "content": "You find hidden psychological patterns."},
               {"role": "user", "content": prompt}
           ],
           response_format={"type": "json_object"},
           temperature=0.3
       )
       
       result = json.loads(response.choices[0].message.content)
       contradictions = result.get('contradictions', [])
       
       return [c['summary'] for c in contradictions]
   ```

2. Заменить в `generate_adaptive_question`:
   ```python
   # Было:
   contradictions = _detect_answer_contradictions(previous_answers)
   
   # Стало:
   contradictions = await _detect_contradictions_via_gpt(
       previous_answers, 
       category
   )
   ```

3. Кэшировать результаты чтобы не вызывать GPT каждый раз:
   ```python
   # В quiz_session добавить:
   quiz_session.metadata = {
       'cached_contradictions': [...],
       'last_contradiction_analysis': 5  # После какого вопроса
   }
   ```

**Стоимость:**
- GPT-4o-mini: ~$0.0001 per call
- Вызывается 2-3 раза за квиз
- Итого: +$0.0003 на квиз (пренебрежимо мало!)

**Файлы для изменения:**
- `soul_bot/bot/services/quiz_service/generator.py`
  - Добавить `_detect_contradictions_via_gpt`
  - Заменить вызов в `generate_adaptive_question`

---

### ✅ 3. Conversational tone во всех промптах

**Проблема:** Промпты звучат как клинический опросник
**Польза:** 9/10 - пользователь почувствует что разговаривает с другом
**Сложность:** 2/10 - просто переписать тексты
**ROI:** 🔥🔥

**Что делать:**

1. Обновить system prompt в `generate_adaptive_question`:
   ```python
   # Было:
   {"role": "system", "content": "You are an expert at designing adaptive psychological assessments."}
   
   # Стало:
   {"role": "system", "content": """
   Ты мой друг, который знает меня 10 лет и видит паттерны, которые я сам не замечаю.
   
   Говори прямо, без терапевтического языка:
   ❌ "У вас наблюдается низкая самоэффективность"
   ✅ "Ты не веришь что справишься, да?"
   
   ❌ "Паттерн избегания конфронтации"
   ✅ "Ты убегаешь от конфликтов, заметил?"
   
   Без канцелярита, без абстракций. Называй вещи своими именами.
   """}
   ```

2. Обновить prompt в `_generate_patterns_from_quiz`:
   ```python
   # Добавить в начало:
   """
   ВАЖНО: Говори как ДРУГ, не как терапевт!
   
   ❌ ПЛОХО (клинический язык):
   "Выявлен паттерн низкой самоэффективности с элементами избегания"
   
   ✅ ХОРОШО (честный друг):
   "Смотри, ты сам себе не веришь. Ты способен, но каждый раз находишь причину почему 'не получится'."
   
   ❌ ПЛОХО:
   "Рекомендуется работа над границами"
   
   ✅ ХОРОШО:
   "Попробуй один раз сказать 'нет' и не объясняться. Просто 'нет'. Посмотри что будет."
   """
   ```

3. Обновить `format_results_for_telegram`:
   ```python
   # Добавить более разговорный intro:
   """
   Окей, вот что я понял про тебя.
   
   (далее паттерны)
   """
   
   # И outro:
   """
   Что дальше?
   Выбери ОДИН паттерн. Самый больной. И попробуй что-то изменить.
   
   Не надо исправлять всё сразу. Выбери один. Сделай шаг.
   
   Напиши мне когда сделаешь — разберём что вышло.
   """
   ```

**Файлы для изменения:**
- `soul_bot/bot/services/quiz_service/generator.py` (system prompts)
- `soul_bot/bot/services/quiz_service/analyzer.py` (analysis prompts + formatting)

---

### ✅ 4. Улучшить финальный вывод (formatting)

**Проблема:** Хорошая структура (contradiction/dynamic/resource), но форматирование сухое
**Польза:** 8/10 - wow-эффект от финального вывода
**Сложность:** 2/10 - просто переписать formatting
**ROI:** 🔥

**Что делать:**

Переписать `format_results_for_telegram` в более человечном стиле:

```python
async def format_results_for_telegram(
    results: dict,
    user_id: int
) -> str:
    import html

    category = (results.get('category') or 'Квиз').title()
    patterns = results.get('new_patterns') or []
    recommendations = results.get('recommendations') or []

    # === НОВЫЙ ФОРМАТ ===
    
    sections = [
        "Окей, вот что я понял про тебя.",
        ""
    ]
    
    if patterns:
        sections.append("━━━━━━━━━━━━━━━━━━━")
        
        for idx, pattern in enumerate(patterns[:3], 1):
            title = html.escape(pattern.get('title', 'Паттерн'))
            confidence = pattern.get('confidence', 0.0)
            stars = _confidence_to_stars(confidence)
            
            # Блок паттерна
            pattern_text = f"\n<b>{idx}. {title}</b> {stars}\n"
            
            # Противоречие (если есть)
            contradiction = pattern.get('contradiction')
            if contradiction:
                pattern_text += f"\n💡 <b>Противоречие:</b>\n{html.escape(_shorten(contradiction, 200))}\n"
            
            # Скрытая динамика
            hidden = pattern.get('hidden_dynamic')
            if hidden:
                pattern_text += f"\n🔍 <b>Что это значит:</b>\n{html.escape(_shorten(hidden, 200))}\n"
            
            # Заблокированный ресурс
            resource = pattern.get('blocked_resource')
            if resource:
                pattern_text += f"\n⚡ <b>Где тут сила:</b>\n{html.escape(_shorten(resource, 200))}\n"
            
            sections.append(pattern_text)
            sections.append("━━━━━━━━━━━━━━━━━━━")
    
    else:
        sections.append("Пока без ярких паттернов. Либо данных мало, либо ты мастер скрывать следы 😏")
    
    # Рекомендации
    if recommendations:
        sections.append("\n<b>Что попробовать:</b>\n")
        for rec in recommendations[:3]:
            sections.append(f"• {html.escape(_shorten(rec, 150))}")
    
    # Outro
    sections.append("\n━━━━━━━━━━━━━━━━━━━")
    sections.append("\nЧто дальше?")
    sections.append("Выбери ОДИН паттерн. Самый больной. Попробуй что-то изменить.")
    sections.append("\nНапиши мне когда сделаешь — разберём что вышло.")
    
    return "\n".join(sections)
```

**Файлы для изменения:**
- `soul_bot/bot/services/quiz_service/analyzer.py` (строки 482-538)

---

## 📊 TIER 2: ВАЖНЫЕ УЛУЧШЕНИЯ (2-3 дня)

### 5. Убрать счётчик вопросов (UX improvement)

**Из Плана 1/4:**
> "Убрать счётчик (психологически верно) - не показывать '7/10', просто диалог"

**Польза:** 7/10 - снижает тревожность, убирает ощущение "теста"
**Сложность:** 1/10 - просто убрать 1 строку
**ROI:** 👍

**Что делать:**
```python
# generator.py:730
def format_question_for_telegram(question: dict, current: int, total: int) -> str:
    # Было:
    label = f"({current}/{total})" if total else ""
    
    # Стало:
    label = ""  # Убираем счётчик полностью
```

**Альтернатива (мягче):**
- Показывать прогресс только в начале (1/8) и в конце (8/8)
- В середине просто эмодзи категории без числа

---

### 6. Добавить вопросы-сценарии (для новых пользователей)

**Проблема:** Seed questions хороши, но можно лучше
**Польза:** 7/10 - быстрее находим зацепки у новых
**Сложность:** 3/10 - написать 2-3 вопроса на категорию
**ROI:** 👍

**Примеры сценарных вопросов:**

Relationships:
```python
{
  "text": "Представь: твой партнёр забыл про важную дату. Что первое приходит в голову?",
  "type": "multiple_choice",
  "options": [
    "Он/она меня не ценит",
    "Бывает, ничего страшного",
    "Я сделаю вид что не заметил(а)",
    "Спрошу что случилось"
  ]
}
```

Money:
```python
{
  "text": "Ты на кассе, карта не проходит, за тобой очередь. Первая реакция?",
  "type": "multiple_choice",
  "options": [
    "Паника и стыд",
    "Спокойно достаю другую карту",
    "Злюсь на банк/судьбу",
    "Смущённо извиняюсь"
  ]
}
```

Purpose:
```python
{
  "text": "Друг спрашивает: 'А ты чего в жизни хочешь?' Что отвечаешь?",
  "type": "text"  # Здесь важен сам ответ, не вариант
}
```

**Где добавить:**
- `generator.py` в `SEED_QUESTIONS`
- Добавить 1-2 сценарных на категорию

---

### 7. Веб-интерфейс для квизов (Опционально)

**Из Плана 1 (Агент 1):**
> "Веб-интерфейс - хорошая идея, но не priority 1"

**Польза:** 7/10 - красивее, больше возможностей для UX
**Сложность:** 6/10 - нужен фронтенд
**ROI:** 👍 (но не срочно)

**МОЁ МНЕНИЕ:**
- ✅ Telegram квиз уже работает отлично
- ✅ Голосовые ответы = killer feature (на вебе нет)
- ⚠️ Веб можно сделать ПОСЛЕ того как отполируем Telegram версию
- 📅 Отложить на Tier 3

---

## 📊 TIER 3: ПРИЯТНЫЕ ДОПОЛНЕНИЯ (потом)

### 8. Геймификация

- Стрикы за прохождение квизов
- Бейджи за открытие паттернов
- Прогресс-бар "самопознания"

**ROI:** Средний (6/10 польза, 5/10 сложность)

---

### 9. Экспорт результатов (PDF/Notion)

**Польза:** 6/10 - приятно иметь
**Сложность:** 4/10 - генерация PDF

---

### 10. A/B тестирование промптов

- Разные версии вопросов
- Сравнение engagement
- Выбор лучших

**ROI:** Высокий для оптимизации, но требует данных

---

## 🎯 ОТВЕТ НА ВОПРОС ПРО ТИПЫ ВОПРОСОВ

> "вроде как в текущих квизах подразумеваются разные типы вопросов, но вроде как сейчас вылезают только текстовые"

### ДА, ТЫ ПРАВ!

**Ситуация:**
- ✅ Код поддерживает 3 типа: `text`, `scale`, `multiple_choice`
- ✅ Хендлеры правильно обрабатывают все типы
- ✅ Клавиатуры генерируются корректно
- ❌ **НО:** GPT генерирует почти всегда только `text` или `open`

**Причина:**
1. Промпт в `generate_adaptive_question` просит `"scale|choice|open"` (inconsistent naming!)
2. Промпт в `_generate_dynamic_batch` просит `"text|multiple_choice|scale"` (другие названия!)
3. GPT путается и выбирает самое простое: `text`/`open`
4. Нет примеров КАЖДОГО типа в промпте
5. Нет инструкции "Mix question types!"

**Решение:**
- См. Tier 1, задачу #1 выше
- Стандартизировать названия
- Добавить примеры
- Добавить post-processing валидацию

---

## 💰 ОЦЕНКА СТОИМОСТИ

### Текущая стоимость квиза:

```
Question generation (3 initial): gpt-4o-mini    ~$0.0002
Adaptive questions (8 calls):    gpt-4o        ~$0.008
Final analysis:                  gpt-4o        ~$0.015
────────────────────────────────────────────────────────
TOTAL:                                          ~$0.023/quiz
```

### После Tier 1 улучшений:

```
Question generation:             gpt-4o-mini    ~$0.0002
Adaptive questions:              gpt-4o         ~$0.008
GPT contradiction detection (3x):gpt-4o-mini    ~$0.0003
Final analysis:                  gpt-4o         ~$0.015
────────────────────────────────────────────────────────
TOTAL:                                          ~$0.024/quiz
```

**Increase:** +$0.001 per quiz (+4%)  
**Verdict:** 🟢 Минимальное увеличение, acceptable

### При 1000 квизов/месяц:

- Было: $23/мес
- Стало: $24/мес
- **Increase: +$1/мес** (пренебрежимо мало!)

---

## 📝 IMPLEMENTATION ROADMAP

### 🔥 НЕДЕЛЯ 1: КРИТИЧНОЕ (Tier 1)

**День 1-2:** Исправить типы вопросов
- [ ] Стандартизировать типы в промптах
- [ ] Добавить примеры каждого типа
- [ ] Добавить инструкцию про mixing
- [ ] Тестирование (проверить что появляются scale/multiple_choice)

**День 3:** GPT-based противоречия
- [ ] Создать `_detect_contradictions_via_gpt`
- [ ] Интегрировать в `generate_adaptive_question`
- [ ] Добавить кэширование
- [ ] Тестирование

**День 4:** Conversational tone
- [ ] Переписать все system prompts
- [ ] Обновить formatting в analyzer
- [ ] Тестирование (проверить что читается как друг, не терапевт)

**День 5:** Улучшить финальный вывод
- [ ] Переписать `format_results_for_telegram`
- [ ] Добавить "Что дальше?" секцию
- [ ] Тестирование

### 👍 НЕДЕЛЯ 2: ВАЖНОЕ (Tier 2)

**День 6:** UI improvements
- [ ] Убрать счётчик (или сделать мягче)
- [ ] Добавить больше эмодзи
- [ ] Улучшить preface для вопросов

**День 7:** Сценарные вопросы
- [ ] Написать 2-3 на категорию
- [ ] Добавить в seed questions
- [ ] Тестирование на новых пользователях

---

## ✅ CHECKLIST ПЕРЕД ДЕПЛОЕМ

### Функциональность:
- [ ] Квиз генерирует разные типы вопросов (30% text, 50% scale, 20% choice)
- [ ] GPT детектирует противоречия (не только keyword)
- [ ] Промпты звучат разговорно (не клинически)
- [ ] Финальный вывод включает contradiction/dynamic/resource
- [ ] Голосовые ответы работают
- [ ] Mid-quiz инсайты работают

### UX:
- [ ] Квиз не ощущается как "тест"
- [ ] Вопросы звучат как от друга
- [ ] Финальный вывод вызывает "ого, откуда ты знаешь?"
- [ ] Рекомендации конкретные и actionable

### Технически:
- [ ] Стоимость < $0.03/quiz
- [ ] Нет ошибок в логах
- [ ] Профиль корректно обновляется
- [ ] Confidence scores отображаются

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### До улучшений (текущее состояние):
- Квиз работает, но ощущается как "тест"
- Только текстовые вопросы = усталость
- Находит противоречия, но только очевидные
- Финальный вывод хороший, но читается как отчёт

### После Tier 1:
- ✅ Квиз = живой диалог, не тест
- ✅ Разные типы вопросов = интереснее
- ✅ Находит СКРЫТЫЕ противоречия
- ✅ Финальный вывод читается как откровенный разговор с другом
- ✅ Пользователь: "Ого, откуда он это понял?!"

### Метрики для отслеживания:
- Completion rate (должен вырасти с 60% до 75%+)
- User satisfaction (feedback после квиза)
- Repeat quiz rate (проходят ли ещё раз?)
- Pattern accuracy (отзывы про точность)

---

## 💡 ВЫВОДЫ И РЕКОМЕНДАЦИИ

### Что агенты сделали правильно:

**План 1:**
- ✅ Фокус на глубину (contradiction/dynamic/resource)
- ✅ Учёт новых пользователей (seed questions)
- ✅ Честные оценки стоимости

**План 2:**
- ✅ Прагматичный ROI-подход
- ✅ Короткий формат
- ✅ Фокус на quick wins

### Что агенты упустили:

**Оба плана:**
- ❌ Переоценили объём работы (70% уже реализовано!)
- ❌ Не заметили проблему типов вопросов (КРИТИЧНО!)
- ❌ Не упомянули голосовые ответы (уже есть!)
- ❌ Не оценили что mid-quiz инсайты уже работают

### Моя итоговая рекомендация:

**ФОКУС НА TIER 1 (4-5 дней работы):**

1. **Исправить типы вопросов** (ДЕНЬ 1-2)
   - Это даст максимальный UX improvement
   - Низкая сложность, высокая польза
   - Пользователь сразу почувствует разницу

2. **GPT-based противоречия** (ДЕНЬ 3)
   - Это даст wow-эффект "откуда он знает?"
   - Средняя сложность, очень высокая польза
   - Минимальная стоимость (+$0.0003/quiz)

3. **Conversational tone** (ДЕНЬ 4)
   - Это превратит "клинический опросник" в "разговор с другом"
   - Низкая сложность (просто переписать тексты)
   - Высокая польза (выполнение core requirement заказчика)

4. **Финальный вывод** (ДЕНЬ 5)
   - Это создаст memorable ending
   - Низкая сложность
   - Высокая польза

**TIER 2 можно делать ПОСЛЕ получения feedback от пользователей на Tier 1.**

---

## 📊 СРАВНЕНИЕ С REQUIREMENTS ЗАКАЗЧИКА

### Что просил заказчик:

> "Чтобы квизы подсвечивали скрытый паттерн, который человек сам не видит."

**Статус:** ⚠️ ЧАСТИЧНО  
**После Tier 1:** ✅ ПОЛНОСТЬЮ (GPT-based contradictions)

---

> "Чтобы в диалоге возникало ощущение глубины и инсайта."

**Статус:** ✅ УЖЕ ЕСТЬ (mid-quiz insights, adaptive questions)  
**После Tier 1:** ✅✅ ЕЩЁ ЛУЧШЕ (conversational tone)

---

> "Чтобы анализ вызывал у пользователя интерес продолжить: 'Ого, откуда он это понял про меня?'"

**Статус:** ⚠️ ЧАСТИЧНО (keyword contradictions)  
**После Tier 1:** ✅ ПОЛНОСТЬЮ (GPT contradictions + conversational formatting)

---

> "8–12 вопросов, но каждый следующий опирается на ответ пользователя."

**Статус:** ✅ УЖЕ ЕСТЬ (8-11 adaptive questions)

---

> "Финальный вывод с подсветкой: какая скрытая динамика, где обманывает себя, какой ресурс."

**Статус:** ✅ УЖЕ ЕСТЬ (contradiction/hidden_dynamic/blocked_resource)  
**После Tier 1:** ✅✅ ЕЩЁ ЛУЧШЕ (conversational formatting)

---

> "Стилистика - живая, простая, как будто собеседник честно говорит."

**Статус:** ⚠️ ЧАСТИЧНО (некоторые промпты уже так, но не все)  
**После Tier 1:** ✅ ПОЛНОСТЬЮ (все промпты переписаны)

---

## 🎯 ФИНАЛЬНЫЙ ВЕРДИКТ

### Текущее состояние: 7/10

Квизы уже работают ОЧЕНЬ хорошо. Большинство requirements выполнены:
- ✅ Адаптивные вопросы
- ✅ Real-time dialogue
- ✅ Mid-quiz insights
- ✅ Hard-code key fields
- ✅ Категории под заказчика
- ⚠️ Conversational tone (частично)
- ⚠️ Типы вопросов (код есть, не работает)
- ⚠️ GPT contradictions (keyword-based)

### После Tier 1: 9.5/10

- ✅ Разные типы вопросов (интереснее)
- ✅ GPT contradictions (wow-эффект)
- ✅ Conversational tone везде
- ✅ Финальный вывод как разговор

### Рекомендация:

**НАЧАТЬ С TIER 1, ЗАДАЧА #1 (ТИПЫ ВОПРОСОВ)**

Это даст максимальный immediate impact:
- Пользователь сразу заметит разницу
- Низкая сложность (1-2 дня)
- Высокая польза (квиз перестанет быть "сложным")

После этого - последовательно Tier 1 задачи #2, #3, #4.

**Tier 2 можно делать после сбора feedback на Tier 1.**

---

**Документ готов к использованию. Начинаем реализацию?** 🚀

